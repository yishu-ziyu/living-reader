/**
 * Shared re-exports for UI-facing source identity.
 * Runtime SourceBlocks come from the OLL adapter, not page constants.
 */

export {
  DOMAIN_SOURCE_IDS,
  type DomainSourceId,
  type SourceBlock,
  type SourceKey,
  type SourceLocator,
  type EvidenceRef,
  type BodyNode,
  type BookArtifact,
} from "@/modules/book/domain";
