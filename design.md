# 设计基线：针厂的边界

> Status：Draft 0.9（D015 已接受；T070 点击切地点被驳回；可走世界嵌入合同待裁决）
> Owner：Codex
> Visual & interaction approver：用户
> Last reviewed：2026-08-10
> Applies to：正式应用 `product/`；`prototypes/` 只承担隔离验证
> Supersedes：Draft 0.6

本文是 Living Reader 的**产品设计宪法与决策索引**。它规定长期体验目标、用户边界、已接受决策和验收方式；不复制 Teable 的任务状态、架构文档的内部协议、某轮浏览器问题清单或完整视觉 token。

文中状态含义：

- **Accepted**：已经形成产品决定，后续实现必须遵循。
- **Proposed**：已有方向，但需要原型、证据或用户判断才能进入 Accepted。
- **Rejected**：已明确排除；重新提出时必须提供新的反证。
- **Open**：尚未形成足以比较的方案。

## 1. 一句话产品设计

**不要把一本书变成一个故事，而是把一本书变成一套可以运行的规律。**

读者从真实原文出发，说出自己的理解，确认思想关系；关系经过证据和可玩性审阅后，在同一阅读界面内长出一个受边界约束的世界。读者行动、看到确定性后果，再沿事件与模型假设回到触发它的原文。

主体验链：

`SourceBlock → ReaderIdea → RelationProposal → Review/Commit → WorldIntent → WorldPlan → Reader Accepts → Compile/Seed → Inline World → DomainEvent → Evidence → SourceBlock`（同一世界可由后续 `ExecutableWorldPatch` 增量扩展）

## 2. 问题、用户与证据

### 用户问题

目标读者不是来获得一段更短的摘要，也不是来和书中角色聊天。他在读到抽象机制时，需要把“我大概懂了”推进到三个更强的状态：

1. 我能指出自己的理解来自哪段原文。
2. 我能看见两个思想之间的方向、约束和不确定性。
3. 我能在受控世界中行动，并用后果检验自己的理解。

### 已观察事实

- 用户明确要求：把书变成可运行规律，而不是自由叙事或聊天冒险。
- 当前产品已能展示真实 `SourceBlock`、保存想法、提出关系候选、生成陪读候选和温和回引。
- 2026-08-09 真实界面审计显示：书页与 Agent 的视觉分区方向成立，但轨道信息过载、两个输入入口竞争、关系与原文空间脱节、跑题卡低对比，360px 视口无法进入正文。
- 用户对当前界面的直接判断是“不够 Apple”。这是一条设计输入，不等于要求复制 Apple 的表面样式。

证据见：[UI/UX 定位审计](docs/design-audit/2026-08-09/report.md)与[design.md 写作调研](research/design-md-writing-research.md)。

### 当前推断

产品的独特价值位于“原文如何长出关系、世界和证据”的内容层。操作层越像工程控制台，读者越难保持阅读心流；操作层越依赖新奇视觉语法，越会与内容竞争。因此应把原创表达集中在书与世界，把 Agent、输入、权限和反馈做得熟悉、克制、可预测。

这项推断需要通过同内容、同逻辑的交互 A/B 和用户判断继续验证。

## 3. 首版目标与非目标

### Goals

首个可演示切片提供 Cannan 版《国富论》Books I–V 的整书章节阅读、中文机译与原文对照；可执行世界仍只围绕 Book I, Chapters I–III 的两个受审机制：

- 分工为什么可能提高生产率。
- 交换怎样让分工继续发生。
- 为什么分工受市场范围限制。
- 读者的理解怎样经过证据、关系和规则，变成可行动、可回放的思想实验。

核心来源块：

- `smith.b1.c1.p1`（兼容别名 `smith.b1.c1.division`）：分工通过熟练、减少切换和机器提高生产率。
- `smith.b1.c3.p1`（兼容别名 `smith.b1.c3.market_extent`）：市场范围限制分工继续细化的程度。
- `smith.b1.c2.exchange`：提供交换语境，但不扩大首版世界范围。

成功的可见信号：读者能从原文出发，形成自己的想法，理解并确认关系，在世界中执行至少一个非装饰性行动，看到确定性后果，再回到同一原文核对完整证据链。

### Non-goals

- 不做宏观经济预测、通用开放世界编辑器或与原文脱钩的自由叙事游戏。
- 不把完整游戏关卡提前写进 recipe，再假装成实时生成。
- 不让 LLM 直接写钱、库存、订单、角色状态、最终关系或可执行代码。
- 不用 NPC 聊天、排行榜、KPI dashboard 或固定视频代替机制体验。
- 不要求用户授权麦克风才能完成核心路径。
- 不把 Apple 感简化为毛玻璃、大圆角、SF 字体或仿制 macOS 设置页。
- 不在根 `design.md` 记录任务排期、负责人变更和当前代码完成度。

## 4. 设计原则与反例

| 状态 | 原则 | 理由 | 可观察规则 | 反例 |
|---|---|---|---|---|
| Accepted | 规律优先于故事 | 产品价值是理解机制并检验后果 | 行动改变确定性状态；结果可解释、可回放 | LLM 即兴续写剧情或结果 |
| Accepted | 书是主角，Agent 是可召回的边缘层 | 阅读注意力是稀缺资源 | 首屏正文是第一视觉重心；任一状态只有一个主动作 | 缩窄轨道但仍常驻全部工程状态 |
| Accepted | 来源先于权威 | 读者需要核对，不是服从回答 | 引文、推断、读者想法和模型扩展一眼可区分 | 把释义或模型假设写成 Smith 原话 |
| Accepted | 用户控制节奏 | Agent 只能辅助理解，不能替用户决定 | 可确认、修改、拒绝、暂缓、停止、撤回和重试 | 未经确认提交关系或自动开启语音 |
| Accepted | 复杂度渐进出现 | 一次展示全部能力会破坏阅读心流 | 默认显示当前一步、必要反馈和下一动作；技术依据按需展开 | 首屏同时展示锚点、语音、Ideas、思考、讨论和关系 |
| Accepted | 操作熟悉，内容独特 | 品牌表达属于内容层，操作层负责可预测 | 控件靠近影响的内容；相同外观保持相同行为 | 为每种 Agent 状态发明不同卡片和手势 |
| Accepted | 反馈连续且可恢复 | 信任来自行为兑现，不来自装饰 | 每个提交、失败和中断都有立即、明确的可见结果 | 只显示“处理中”或制造假成功 |
| Accepted | 精致来自取舍 | Apple 感首先是清晰、邻近、可读和响应 | 无溢出、无低对比、触达稳定、动效有因果目的 | 用玻璃拟态遮盖层级和文案问题 |

## 5. 端到端体验状态

| 阶段 | 用户行动 | 系统状态与可见结果 | 退出、失败与恢复 |
|---|---|---|---|
| 进入阅读 | 打开书并定位章节/段落 | 中文译文优先，逐段标注“机译”，原文、版本和稳定来源可展开核对；不预先请求麦克风 | 可纯文字阅读；来源失败时说明缺失，不伪造书页 |
| 表达理解 | 输入文字或主动开启语音 | 显示系统听到/理解了什么；只有 final turn 可形成 `ReaderIdea` | partial、取消和低置信不写入；可修改、重说、停止 |
| 核对理解 | 查看引文与推断 | quote、inference、experiment 类型和不确定性可见 | 可拒绝、修订或保留为未决候选 |
| 连接关系 | 审阅 `RelationProposal` | 用人话解释方向、依据、影响和不确定性 | 接受、拒绝、修改或暂缓；候选不参与编译 |
| 邀请与预览 | 查看 Agent 实时提出的 WorldIntent/WorldPlan | 触发问题、来源、将出现的地点/角色/动作、规则边界、模型扩展和可返回路径可见；邀请本身零世界写入 | 读者可拒绝或暂缓；来源不匹配、Gate 失败或 basis 过期时世界不出现 |
| 世界展开 | 明确接受邀请 | 计划经确定性编译后，世界在相关原文之间真实展开；空间节点与当前可行动作可见，来源连接仍可理解 | 可收起并回到原位置；加载与展开不造成跳页 |
| 行动反馈 | 执行当前 allowlist / ActionCandidate 内行动 | 状态变化、约束、失败原因和二阶后果可见；角色观察按因果顺序解释已发生事实 | 可停止、重试或换方案；未知输入不改世界状态 |
| 世界扩展 | 追问并确认 ExecutableWorldPatch | 同一 world_id 上增量解锁地点、角色、动作或约束，不另开无关小游戏 | 拒绝补丁则世界保持原边界；补丁必须可回放 |
| 返回证据 | 结束或收起世界 | 按想法→关系→计划/补丁→事件→原文回放 | 可打开详细依据；模型简化不冒充来源结论 |

## 6. 内容身份与世界边界

### 不可混淆的身份

| 层 | 代表对象 | 必须表达的来源 | 不允许做什么 |
|---|---|---|---|
| 书籍 | `BookArtifact` / `SourceBlock` / `SourceAnchor` | 版本、稳定语义锚点、可定位证据 | 用页码代替稳定 ID；把释义冒充引文 |
| 读者 | `ReaderIdea` | 读者自己的观察、问题、假设或类比 | Agent 替读者发言或改写成事实 |
| Agent | `BookThought` | 类型、证据、置信度和修订记录 | 保存隐藏思维链；把推断写成原话 |
| 关系 | `RelationProposal` / `RelationEdge` | 候选/确认状态、方向、类型和证据 | 未经确认参与世界编译 |
| 模型 | `ModelExtension` / 阅读图 `WorldPatch` / 可执行 `ExecutableWorldPatch` | 来源规则与运行假设的边界；两类补丁不可混用 | 把模型扩展伪装成 Smith 的主张；把阅读关系补丁当成世界改关 |
| 世界 | `WorldIntent` / `WorldPlan` / `WorldState` / `DomainEvent` | 意图与计划可审阅；状态与事件确定性可回放 | 让 LLM 直接写核心状态；用完整预制关卡冒充实时生成 |
| 回放 | `EvidenceBlock` / `ReadingTrace` | 用户行动、事件、来源和边界 | 只给总结，不给可核对因果链 |

PDF 页码只负责显示位置，不能作为永久身份。

### 首版世界

首版可执行世界仍围绕羊毛贸易/工坊机制，但**不是**固定四站流水线罐头：

- 预制：经济原语、规则能力、资产语法、安全预算，以及可选的受审机制种子。
- 实时：Agent 根据当前 `SourceBlock`、已确认关系和读者问题生成 `WorldIntent` 与可审阅 `WorldPlan`；随后可用 `ExecutableWorldPatch` 扩展同一世界。
- 最小状态仍包括市场可触达订单、交换是否开放、运输/路径条件、库存、订单、现金、专业化深度和角色局部状态。
- 读者必须看见：专业化可能提高产出，但市场过小时会造成等待、积压、现金压力或拒绝深化；追问可以把封闭工坊扩到道路与更大市场。

世界是有明确简化条件的思想实验，不是现代经济预测。同一 plan/patch revision、seed、ruleset 和行动序列必须 exact replay。

详细交互合同见 [`docs/architecture/realtime-agent-world-interaction.md`](docs/architecture/realtime-agent-world-interaction.md)。

## 7. 信息架构与关键交互合同

### 内容层与操作层

- **内容层**：原文、读者想法、关系、Agent 的可核对回应、Inline World 和 Evidence。这里承载品牌、节奏与原创表达。
- **操作层**：导航、Agent 召回、输入、状态、权限、错误和技术依据入口。这里优先熟悉、克制与可预测。

技术 ID、hash、graph revision 和 provider 状态不得成为默认阅读内容；必要时通过“查看依据”访问。来源和不确定性不能以渐进揭示为借口被隐藏。

### 关键交互

#### 阅读定位

- 进入条件：已有可验证 `SourceBlock`。
- 主动作：阅读、选择或围绕当前段落表达。
- 可见反馈：当前位置、版本与来源可核对。
- 恢复：关闭世界或证据后返回同一段，不回首页、不漂移。

#### 表达入口

- **Proposed**：每个原文上下文默认只有一个主要表达入口。
- 提交后再用轻量确认区分问题、观察、假设和类比；UI 合并不等于领域身份合并。
- 文字路径完整可用；主动语音只是等价入口。

#### Agent 回应与关系审阅

- 人类可读的引文、推断、开放问题和关系候选靠近触发原文出现。
- Agent 轨道只保留摘要、未决状态和召回入口。
- 接受、拒绝、修改和暂缓都在原位反馈，不让读者跨两个滚动容器寻找结果。

#### 世界展开与证据回归

- 关系未确认或 Gate 未通过时，世界不进入开放态。
- 展开前先预览 WorldIntent/WorldPlan；展开时呈现可区分的地点节点与当前行动，而不是一条不可游走的工序条。
- 世界收起后回到触发位置；Evidence 能从事件逐层返回原文。
- 改变世界边界的补丁需要再次可见确认；低风险 allowlist 动作可直接执行。

#### 语音、权限与停止

- 麦克风、语音播放、文字阅读和后台提醒分别授权，并在使用情境中解释原因。
- Gate 通过前不主动邀请语音；拒绝权限后仍可完成文字主链。
- `停止` 必须真正停止采集或播报；已提交想法和世界事实保留。
- 超时、断线、低置信、未知和版本冲突停在可见的待确认或可重试状态。

## 8. 视觉语言

### “更 Apple”的项目翻译

这里的 Apple 感不是平台皮肤，而是以下可观察品质：

- **清晰层级**：正文先于系统；一个状态只有一个主动作。
- **内容邻近**：控件、反馈和解释靠近其影响的原文或对象。
- **克制披露**：先给人的判断，再提供技术依据；不展示无行动价值的状态。
- **一致行为**：同样的控件、位置和动效保持同样含义。
- **可读与可触达**：关键文字不依赖极小字号；对比清楚；触控目标按 Apple 建议至少 44×44pt；长内容不溢出。
- **响应与连续**：操作立即反馈；动效只解释状态或空间因果，短、可中断，并有 reduced motion 等价状态。
- **留白服务理解**：留白用于分组和阅读节奏，不用于制造空洞的“高级感”。

### 设计前必查入口

- 所有 UI、UX、交互与 Motion 工作开始前，必须先打开 [UI Skills](https://www.ui-skills.com/skills)，按当前任务选择最窄的相关技能；不能只凭现有界面、通用灵感图或记忆开始设计。
- Motion 是项目的重点品味轴。至少先检查 [Motion Skills](https://www.ui-skills.com/skills/motion)，再按任务选用 Apple Design、动画词汇、动效机会、动画审查、性能与无障碍等具体技能。
- 追求 Apple 式体验时，优先阅读 [Apple Design](https://www.ui-skills.com/skills/emilkowalski/apple-design)，把流体手势、连续空间、速度继承、可中断弹簧、反馈、克制和 reduced motion 翻译为本项目的用户行动与可见结果；不得退化成表面仿制。
- UI Skills 是设计发现与执行参考，不替代本文的 Accepted 决策、真实产品证据和用户的最终视觉与交互裁决。

### 书页作为交互画布

- **Accepted**：正式阅读页是一张连续、可选择文字的书页，不用白色卡片或控制台框架承载正文。
- `ReaderIdea` 以页边批注贴住对应句段；`RelationProposal` 以编辑校样式括号和短引线连接两处来源。
- 关系确认后，Inline World 作为书中的可运行图版从两段正文之间连续展开，并把下方内容自然推开。
- 不设置承载锚点、转写、想法和关系的密集常驻侧栏；这些结果归位到触发它们的原文附近。
- Agent 静止时以当前段落页边的小 Orb 保持可召回，不退化为普通书签抽屉。
- 用户进入真实 Agent 对话后，完整保留 Thinking Orbs 的待机、连接、聆听、思考、说话、Stop 和取消体验；书页留在背后提供当前来源语境。
- 对话结束后，结果收回对应原文旁，Orb 回到安静状态。

### 基础配色与排版

- **Accepted**：页面采用冷白纸、石墨正文和低饱和墨蓝强调，不沿用参考图的深绿控制台配色；精确色值由同内容视觉样例校准。
- 章节、页码、引文和正文使用书籍级衬线排版；操作标签与状态使用少量现代无衬线。
- 任一状态只有当前一步使用强调色；来源、批注、关系和模型扩展依靠位置、线型与文字身份区分，不能只靠颜色。
- 页面精致感来自字级、行距、版心、页边距、括号和引线的秩序，不来自卡片、阴影、玻璃或大圆角。

### 动效分工

- 普通阅读和滚动保持安静。
- 想法保存后，页边批注从对应句段的括号处显现；关系确认时，引线按来源方向连接两段原文。
- 世界展开通过连续增加两段正文之间的纸面空间表达，不使用遮盖正文的弹窗或突然跳屏。
- Thinking Orbs 独占高表现力的角色与语音动效；书页动效只解释来源、关系和空间变化。
- 所有展开可中断，关闭后恢复原句位置；reduced motion 直接进入可理解的终态。

### 阅读与世界画面

- 真实 PDF/文本层是主画面，不用不可选择的截图伪造书页。
- 世界必须让目标段落后的内容真实下移，不能只覆盖在 PDF 上。
- 主世界不使用现代 dashboard KPI 卡、玻璃拟态、渐变按钮或 Emoji/SVG 人物作为最终视觉。
- **Proposed**：世界采用插入书页的可走图版；角色在空间中移动到达地点，而不是点列表切换“我在哪”；结构层必须处理占用与深度排序，禁止人物/建筑/标签互相遮死；资产可用 PNG/WebP；最终渲染适配器待 D016 裁决（薄 WalkView 为主，引擎仅原型验证）；绝不以 SVG 作为世界视觉。
- 角色靠外轮廓、工具和动作识别，不能只靠职业标签、颜色或 tooltip。

### 可访问性底线

核心链必须有文字和键盘等价路径；关键状态不能只依赖颜色、位置、声音或动画。Canvas/WebGL 世界需要同步 DOM 摘要或等价行动面板，并支持 200% 缩放和 reduced motion。本文规定设计底线，不替代后续真实键盘、屏幕阅读器和浏览器验收。

## 9. 决策记录与备选方案

| ID | 状态 | 问题 | 当前决定/方案 | 被拒绝或待比较方案 | 理由与证据 |
|---|---|---|---|---|---|
| D001 | Accepted | 书如何变成体验 | 运行可观察规律 | 聊天优先、自由叙事冒险 | 用户明确原则；可重放世界合同 |
| D002 | Accepted | 品牌表达放在哪里 | 内容层原创，操作层熟悉 | 每个系统状态都定制卡片 | 保护阅读注意力与可预测性 |
| D003 | Accepted | 工程证据如何出现 | 人类判断在前，技术依据按需展开 | hash、revision、provider 常驻 | 2026-08-09 UI 审计 |
| D004 | Accepted | 桌面 Agent 形态 | 当前段落页边小 Orb 负责安静召回；进入对话后展开完整 Thinking Orbs | 密集常驻轨道、普通书签抽屉 | 用户要求保留已验收的 Thinking Orbs，同时排除参考图左侧的信息拥挤 |
| D005 | Open | 移动端 Agent 容器 | 正文单栏，Agent 可召回 | 底部层 vs 侧向抽屉 | 先比较可读面积、返回位置与键盘行为 |
| D006 | Accepted | 关系与回应出现在哪里 | 靠近触发原文；页边 Orb 只保留召回和当前对话状态 | 全部挤在固定窄轨道 | 内容邻近原则与真实审计 |
| D007 | Proposed | 世界最终美术与空间形态 | 书页内可走、无遮挡的 1-bit/版画图版；移动到达地点；PNG/WebP 资产 | 四站流水线、点击切地点面板、重叠遮挡场景、SVG 主视觉、dashboard | 用户 2026-08-10 驳回 T070 遮挡与点击切换；详见 walkable-world-embedding.md |
| D008 | Proposed | 原文表达入口 | 一个主入口，提交后确认意图 | 两个同权大输入框 | 减少选择成本，但必须保持领域身份 |
| D009 | Rejected | 如何获得 Apple 感 | 以清晰、邻近、触达、反馈和取舍验收 | 毛玻璃、大圆角、仿 macOS 皮肤 | Apple 官方设计指导与用户反馈 |
| D010 | Accepted | 设计工作从哪里开始 | UI Skills 为设计前必查入口；Motion 与 Apple Design 优先 | 只看现有 UI、通用灵感图或凭记忆设计 | 用户明确指定；该站提供可执行的设计工程技能 |
| D011 | Accepted | 阅读与交互如何共面 | 书页作为交互画布；批注和关系贴住原文；世界在段间展开 | 卡片流、控制台分栏、覆盖正文的弹窗 | 用户选择“活页批注”，并确认文字、交互、文字的连续结构 |
| D012 | Accepted | 页面基础色温 | 冷白纸、石墨正文、低饱和墨蓝强调 | 深绿控制台、泛黄复古滤镜 | 用户明确不采用参考图配色，并选择“冷白墨蓝” |
| D013 | Accepted | 整书与首批世界的范围如何区分 | Cannan 两卷 Books I–V 全部可读；只有两份受审 Book I 配方可邀请进入世界 | 只保留两段、或对全书自动生成世界 | 整书阅读与受审机制库是两个不同的授权边界 |
| D014 | Accepted | 译文如何与原文共存 | 中文机译为主阅读层，逐段诚实标注，原文一键对照；引文/Evidence 始终指向英文原文 | 把机译写回原文、以 gloss 冒充译文 | 读者优先可读与来源可核对必须同时成立 |
| D015 | Accepted | 世界如何实时生成且保持开放 | 预制原语 + 实时意图/计划/补丁；语音只作入口；Kernel 独写事实；空间主交互改为可走到达（见 D016） | 预制整关；LLM 直写状态；有语音=已生成 | 用户 ok 机制；空间执行被 T070 纠正 |
| D016 | Proposed | 可走世界如何嵌入产品 | 薄 WalkView 消费 PresentationPlan；移动/碰撞/深度排序在呈现层；引擎可在 prototypes 验证；EventStore/Kernel 权限不变；侧栏地点列表不得当主空间交互 | 完整 RPG 框架进主包；点击切地点冒充开放；无 DOM 摘要的裸 canvas | T070 驳回 + 开源调研；见 docs/architecture/walkable-world-embedding.md |

## 10. 验收与反证

### 核心产品链

- 读者从真实 `SourceBlock` 开始，并留下归属于自己的 `ReaderIdea`。
- 关系在用户确认前始终是候选态，确认后有版本和证据。
- 世界由本次已确认关系与读者明确接受的实时 WorldPlan 编译，不是固定演示关卡。
- 至少一个非装饰性行动改变 `WorldState`，并产生可解释、可回放事件。
- 同一世界支持至少一次可见的增量扩展（新地点/动作/约束），且不丢失既有状态。
- 失败、停止、权限拒绝、重试和未知分支都有可见结果。
- 世界结束后回到原文，并显示完整 Evidence 链与模型扩展。

### 体验验收

- 首屏正文是第一视觉重心，用户无需先理解 Agent OS 架构。
- 任一状态有一个明确主动作，并能回答“我在哪里、发生了什么、如何离开”。
- 关系、回应和反馈靠近触发内容；技术依据默认折叠但可访问。
- 360px 宽度正文先出现，无页面级横向滚动；桌面和移动保持同一心智模型。
- 关键触控目标至少 44×44pt；无低对比关键文字；200% 缩放仍可完成主链。
- 键盘、文字路径与 reduced motion 不绕过领域合同。
- 用户对关键品味轴作出明确判断；视觉截图不能替代鼠标、滚动、展开、返回和失败路径验收。
- 正文保持连续书页排版；批注、关系和世界都能指出自己贴住的原文位置。
- Thinking Orbs 的状态语义、Stop、取消和 reduced motion 合同在新阅读壳中完整保留。

### 推翻当前设计的信号

出现以下任一情况，不能宣称设计成立：

- 读者把 Agent 轨道理解成主产品，而不是阅读辅助层。
- 读者需要在多个同权入口中先理解系统分类才能表达。
- 世界展开后无法指出它来自哪段原文、哪条关系或哪些模型假设。
- 同一行动序列不能 exact replay，或 LLM 能绕过确认直接改世界。
- 停止、拒绝、权限失败或断线没有明确可恢复结果。
- 只有 build、路由或截图证据，没有真实可见用户链。

详细工程验收以 [`docs/acceptance.md`](docs/acceptance.md) 和 [`docs/prototype-v2-acceptance.md`](docs/prototype-v2-acceptance.md) 为准。

## 11. 未决问题与裁决门槛

| 问题 | Owner / approver | 所需证据 | 裁决门槛 | 阻塞范围 |
|---|---|---|---|---|
| D005 移动端容器 | Codex / 用户 | 360×800 底部层与侧抽屉样例；键盘和返回位置 | 正文先出现、无横向滚动、Agent 可恢复 | T018 |
| D007 世界画风与可走空间 | Codex / 用户 | 可走无遮挡关键帧；化身移动到地点 | 1 秒内分清人/房；不是点列表 | T071 后 T072 |
| D008 单一表达入口 | Codex / 用户 | 问题、观察、假设、取消、修改和低置信语音样例 | 降低选择成本且不混淆领域身份 | T021 |
| D016 可走嵌入方案 | Codex / 用户 | 开源比较 + 嵌入合同 + 最小可走定义 | 用户确认主路径与原型技术 | T071 |
| PDF 段间展开 | Codex / 用户 | 保持逻辑不变的 compositor A/B；滚动锚定与返回测试 | 世界真实推开内容且关闭后回到同一段 | 生产 PDF compositor |
| 最终对外名称 | 用户 | `针厂的边界`、`Living Reader` 与层级方案 | 名称与首版体验一致，不制造错误品类预期 | 对外演示与发布文案 |

世界动作 allowlist、provider、部署和 schema 等技术决策由对应架构文档与 Teable 任务管理，不在此表伪装成视觉决策。

## 12. 文档治理、权威关系与变更记录

### 更新规则

- 产品方向、长期体验边界或 Accepted 决策变化：先更新本文，再改实现合同。
- 新方案仍待比较：标为 Proposed/Open，不写成既定事实。
- 仅实现状态、负责人、优先级或阻塞变化：更新 Teable，不修改本文。
- 某轮 UI 问题、截图和代码落点：写入 `docs/design-audit/` 与 Teable。
- schema、事件、权限和运行时细节：写入 `docs/architecture/` 或 ADR。
- Accepted 决策若被推翻，必须保留原决定、反证和替代理由，不能只覆盖结果。

### 权威关系

1. 本文：产品体验、视觉原则、已接受设计决策和设计验收。
2. [`CONTEXT.md`](CONTEXT.md)：术语、身份和核心不变量。
3. [`docs/product-brief.md`](docs/product-brief.md)：首版产品范围与体验合同。
4. [`docs/architecture/agent-os-behavior-protocol.md`](docs/architecture/agent-os-behavior-protocol.md)：Agent OS 语义权限。
5. [`docs/architecture/realtime-agent-world-interaction.md`](docs/architecture/realtime-agent-world-interaction.md)：实时世界生成、开放交互与增量扩展。
6. [`docs/architecture/event-protocol.md`](docs/architecture/event-protocol.md)：事件、写入、投影、幂等和回放。
7. [`docs/architecture/voice-native-executable-book.md`](docs/architecture/voice-native-executable-book.md)：语音、Inline World 与 PlayabilityGate。
8. [`docs/acceptance.md`](docs/acceptance.md)：工程完成门槛。
9. Teable：任务范围、状态、负责人、依赖和验收证据。
10. [`product/README.md`](product/README.md)：当前实现切片与运行方式。

### 变更记录

| 版本 | 日期 | 变化 | 理由 |
|---|---|---|---|
| Draft 0.1 | 2026-08-09 | 建立产品主线、身份、世界和权限边界 | 形成首份项目设计基线 |
| Draft 0.2 | 2026-08-09 | 加入品味基线、交互节奏与当前 UI 问题 | 将设计笔记和真实界面审计落到项目 |
| Draft 0.3 | 2026-08-09 | 重构为设计宪法与决策索引；加入证据、Goals/Non-goals、状态、反例、方案比较、反证、Owner 和治理；移出易变代码清单 | 采用一手资料调研结论，并响应用户确认结构 |
| Draft 0.4 | 2026-08-09 | 将 UI Skills、Motion Skills 与 Apple Design 设为设计前必查入口 | 用户明确指定；把 Motion 偏好与 Apple 设计方法落成长期工作约束 |
| Draft 0.5 | 2026-08-09 | 确认“活页批注”书页交互、冷白墨蓝配色、段间版画世界与 Thinking Orbs 聚焦对话态 | 用户以参考图中部为样式目标，排除深绿配色和密集左栏，并要求保留已验收的 Agent 对话体验 |
| Draft 0.6 | 2026-08-09 | 锁定整书中文阅读与两份受审配方的分层范围；加入邀请和读者接受边界 | T053 已批准“全书 + 2 配方闭环”，需消除 Draft 0.5 将整书列为非目标的冲突 |
| Draft 0.7 | 2026-08-10 | 将世界从“选受审配方”升级为“实时 WorldIntent/WorldPlan + 增量 Patch”；定义开放四维；回退四站罐头作为 D007 代表 | 用户指出中间世界不够开放，且预制完整游戏不符合实时 Agent 交互 |
| Draft 0.8 | 2026-08-10 | 接受 D015；明确实时语音定位与中文术语；下一刀从地点拓扑开始指哪打哪 | 用户确认机制（ok） |
| Draft 0.9 | 2026-08-10 | 驳回点击切地点；改为可走世界；补充开源调研与产品嵌入合同 D016 | 用户指出严重遮挡，并要求可移动世界与先调研开源 |

## 相关资料

- [design.md 写作调研](research/design-md-writing-research.md)
- [2026-08-09 UI/UX 定位审计](docs/design-audit/2026-08-09/report.md)
- [UI Skills 设计工程技能库](https://www.ui-skills.com/skills)
- [UI Skills：Motion](https://www.ui-skills.com/skills/motion)
- [UI Skills：Apple Design](https://www.ui-skills.com/skills/emilkowalski/apple-design)
- [项目说明](README.md)
- [产品简报](docs/product-brief.md)
- [验收合同](docs/acceptance.md)
- [交付约束](docs/delivery-constraints.md)
- [职业美术方向](research/role-art-direction.md)
